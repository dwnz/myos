## A "virtual OS written in C# with a Javascript application runtime environment"

A rough as in progress expirmental build of a "virtual OS" with an application runtime environment.


#ItWorksOnMyMachine